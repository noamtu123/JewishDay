.gradle/
.DS_Store
.idea/
build/
local.properties
*.iml
captures/
externalNativeBuild/
.cxx/
**/build/
.kotlin/
legacy-reference/

# Signing — keep secrets and build artifacts out of version control
release.jks
*.jks
*.keystore
keystore.properties
*.apk

# Stray session log artifacts
session-ses_*.md

# Local email/avatar export
gmail-icon.png

# AI handoff log — local communication between AI assistants, never pushed
AI_HANDOFF.md

# Claude project instructions — local AI context, like AI_HANDOFF.md
CLAUDE.md

# Personal task runner (hardcodes this machine's JAVA_HOME path)
justfile
