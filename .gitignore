.gradle/
.DS_Store
.idea/
build/
local.properties
*.iml
captures/
externalNativeBuild/
.cxx/
**/build/
.kotlin/

# Signing — keep secrets and build artifacts out of version control
release.jks
*.jks
*.keystore
keystore.properties
*.apk

# Stray session log artifacts
session-ses_*.md

# Local email/avatar export
gmail-icon.png

# Claude project instructions — local AI context, never pushed
CLAUDE.md

# Personal task runner (hardcodes this machine's JAVA_HOME path)
justfile

# Release notes drafts. English is published to GitHub; the Hebrew translation rides along in the
# HTML comment the app reads, so it never renders on the release page. The drafts themselves stay
# local so no Hebrew lands in the repo either.
release-notes/

# Claude Code — share project settings, keep local overrides out
.claude/settings.local.json
